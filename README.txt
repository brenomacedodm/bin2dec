This is a python binary to decimal calculator.
User stories:
	user can enter up to 8 binary digits in one input field
	user must be notified if anything othar than 0 and 1 was entered
	user view the results in a single output field containing the decimal (base 10) equivalent of the binary number that was entered
	user can enter a variable number of binary digits